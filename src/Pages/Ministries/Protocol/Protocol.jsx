import React from "react";
import MinistryLayout from "../MinistryLayout";
import { protocolMinistryData } from "../MinistryData";

const Protocol = () => {
  return <MinistryLayout data={protocolMinistryData} />;
};

export default Protocol;
